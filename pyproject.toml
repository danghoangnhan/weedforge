[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "weedforge"
# Single source of truth: the version is read from Cargo.toml's [package] version by
# maturin. Do not add a static `version` here — bump it in Cargo.toml instead.
dynamic = ["version"]
description = "Rust-first, Python-friendly SDK for SeaweedFS"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
keywords = ["seaweedfs", "storage", "distributed", "s3", "blob"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Rust",
    "Topic :: System :: Distributed Computing",
    "Topic :: System :: Filesystems",
]

[project.urls]
Homepage = "https://github.com/danghoangnhan/weedforge"
Repository = "https://github.com/danghoangnhan/weedforge"
Documentation = "https://github.com/danghoangnhan/weedforge#readme"

[tool.maturin]
features = ["python"]
module-name = "weedforge"
